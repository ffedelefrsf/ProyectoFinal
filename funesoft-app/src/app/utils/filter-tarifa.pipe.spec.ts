import { FilterTarifaPipe } from './filter-tarifa.pipe';

describe('FilterTarifaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTarifaPipe();
    expect(pipe).toBeTruthy();
  });
});
